interface:
  display_name: "MIMIC Phenotyping"
  short_description: "Build or audit reproducible MIMIC phenotypes"
  default_prompt: "Use $mimic-phenotyping-pipeline with the requested build or audit operation for this MIMIC phenotyping analysis."
