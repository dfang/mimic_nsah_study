interface:
  display_name: "MIMIC Reproducibility Release"
  short_description: "Build or audit reproducible MIMIC research releases"
  default_prompt: "Use $mimic-reproducibility-release with the requested build or audit operation for this MIMIC study release."
